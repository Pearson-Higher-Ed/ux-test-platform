{
    "name": "local-web-server",
    "version": "1.0.0",
    "local-web-server": {
        "port": "8000",
        "forbid": "*.json"
    },
    "scripts": {
        "start": "ws",
        "copy-assets": "mkdir -p fonts && mkdir -p icons && cp ./node_modules/pearson-elements/dist/fonts/* ./fonts && cp ./node_modules/pearson-elements/dist/icons/* ./icons && cp -R ./node_modules/@pearson-components/app-header/images ./src/main/java/origamiV2",
        "delete-assets": "find . -type f -name '*.css' -delete && find . -type f -name 'dist*.js' -delete && find . -type f -name 'div*.js' -delete",
        "devDependencies": {
            "local-web-server": "^1.2.6",
            "pearson-elements": "^1.0.6",
            "@pearson-components/app-header": "^2.0.3"
        }
    }
}